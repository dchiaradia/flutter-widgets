import 'dart:ui';

import 'package:flutter/material.dart';
import 'package:my_widgets/src/widgets/config.dart';

class MyColors {
  //Palette Colors
  Color black = mainBlackColor;
  Color dark = mainDarkColor;
  Color medium = mainMediumColor;
  Color soft = mainSoftColor;
  Color white = mainWhiteColor;

  //Info Colors
  Color danger = mainDangerColor;
  Color info = mainInfoColor;
  Color confirm = mainConfirmColor;
  Color alert = mainAlertColor;

  //Enable e Disable Colors
  Color enable = mainEnableColor;
  Color disable = mainDisableColor;
}
